apiVersion: v1
kind: Secret
metadata:
  name: anthropic-secret
  namespace: time-series-indexing
  labels:
    app: llm-services
    component: secrets
type: Opaque
stringData:
  api-key: "${ANTHROPIC_API_KEY}"  # Will be replaced with actual key during deployment