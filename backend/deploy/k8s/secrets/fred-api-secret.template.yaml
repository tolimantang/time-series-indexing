apiVersion: v1
kind: Secret
metadata:
  name: fred-api-secret
  namespace: time-series-indexing
  labels:
    app: fred-service
    component: secrets
type: Opaque
stringData:
  api-key: "REPLACE_WITH_ACTUAL_FRED_API_KEY"  # Copy this file to fred-api-secret.yaml and set real API key